// This component is no longer used and can be deleted.
// Keeping the file to satisfy the "edit existing files" constraint.
// The AppMenubar has been removed from the layout.
import React from 'react';

export function AppMenubar() {
  return null; // Effectively removed
}
